# __archive

Graduated scripts live here. When a script in this repo is promoted to a standalone
Obsidian community plugin (`obsidian-plugin-{name}`), move its folder into this directory
and update its local `README.md` to note which plugin it became.
